apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-cm
  labels:
    app: {{ .Chart.Name }}
data:
  default.conf: |-
    server {
      listen       80;
      listen  [::]:80;
      server_name api-develop.clinicasanjavier.cl;
      server_tokens off;

      proxy_set_header Origin $http_origin;
      proxy_set_header Referer $http_referer;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Server $host;
      proxy_set_header X-Host $http_host;
      add_header Access-Control-Expose-Headers Date;

      # location ~ metrics {
      #     deny  all;
      #     return 404;
      # }

      # location ~ swagger {
      #     deny  all;
      #     return 404;
      # }
      
      # location ~ health {
      #     deny  all;
      #     return 404;
      # }
    }

    server {
      listen       80;
      listen  [::]:80;
      server_name       clsj-desarrollo api-dev.devopschile.cl;
      # server_name     clsj-desarrollo;
      server_tokens off;

      proxy_set_header Origin $http_origin;
      proxy_set_header Referer $http_referer;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Server $host;
      proxy_set_header X-Host $http_host;
      add_header Access-Control-Expose-Headers Date;


      # location /api/auth/ {
      #   if ($request_method = OPTIONS ) {
      #     add_header 'Access-Control-Max-Age' 86000;
      #   }
      #   proxy_pass http://dds-identity-adem-api.dds-ws/;
      #   proxy_http_version 1.1;
      # }

      location / {
        # root  /usr/share/nginx/html;
        proxy_pass http://nginx/;
      }

    }
