# Default values for nginx.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: pcarrascog/click2call
  pullPolicy: IfNotPresent
  ## Overrides the image tag whose default is the chart appVersion.
  tag: latest 
  
imagePullSecrets: #["regcred"]
  - name: regcred
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: #{}
  # kubernetes.io/change-cause: "image updated to 1.16.1"
  # configmap.reloader.stakater.com/reload: "dds-logging"
  # configmap.reloader.stakater.com/reload: "dds-logging,dds-transfers-intranet-adem-cm"
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000
  
service:
  type: ClusterIP
  port: 80
  targetPort: 80

ingress:
  enabled: false
  className: ""
  annotations:
    kubernetes.io/ingress.class: ""
    # nginx.org/redirect-to-https: "true"
    # kubernetes.io/tls-acme: "true"
    #kubernetes.io/ingress.class: "gce-internal"
    #kubernetes.io/ingress.allow-http: "false"
  hosts:
    - host: dev.totalmagnesiano.com.ar
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
    # - secretName: dds-b2b-tls
    #   hosts:
    #     - dds-b2b

resources: # {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  limits:
    cpu: 125m
    memory: 128Mi
  requests:
    cpu: 20m
    memory: 10Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 2
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

# Additional volumes on the output Deployment definition.
volumes: []
# - name: foo
#   secret:
#     secretName: mysecret
#     optional: false

# Additional volumeMounts on the output Deployment definition.
volumeMounts: []
# - name: foo
#   mountPath: "/etc/foo"
#   readOnly: true

nodeSelector: {}

tolerations: []

affinity: {}

configMapData:
  ASPNETCORE_URLS: http://+:8080
  # # DDS_ConnectionStrings__DefaultConnection: "server=10.23.80.12;port=3306;database=identity_adem;user=identity_adem;password=1Y.@V|vt(9y57UV:;"
  # DDS_ConnectionStrings__DefaultConnection: "Server=BASIS-SRDEDB.drogdelsud.com.ar;Database=Adem-Transfers;User Id=Gestion-Transfer-Adem;Password=KR8exfRJ6y6jYcFZcLbW;TrustServerCertificate=True;"
  # DDS_ConnectionStrings__DefaultConnectionDbTypeName: sqlserver
  # DDS_ConnectionStrings__EventBus: "35.223.54.136"  
  # DDS_ConnectionStrings__EventBusPort: ""
  # DDS_RabbitMQSettings__MainQueue__ExchangeName: Novedades_transfer
  # DDS_RabbitMQSettings__MainQueue__ExchangeType: fanout
  # DDS_RabbitMQSettings__MainQueue__MensajeriaType: rabbitmq
  # DDS_RabbitMQSettings__MainQueue__PrefetchCount: "50"
  # DDS_RabbitMQSettings__MainQueue__PrefetchSize: "0"
  # DDS_RabbitMQSettings__MainQueue__QueueName: Novedades_transfer_onpremise
  # DDS_RabbitMQSettings__NewQueue__ExchangeName: Novedades_nuevotransfer
  # DDS_RabbitMQSettings__NewQueue__ExchangeType: fanout
  # DDS_RabbitMQSettings__NewQueue__MensajeriaType: rabbitmq
  # DDS_RabbitMQSettings__NewQueue__PrefetchCount: "50"
  # DDS_RabbitMQSettings__NewQueue__PrefetchSize: "0"
  # DDS_RabbitMQSettings__NewQueue__QueueName: Novedades_nuevotransfer_onpremise

# configMapObject: [dds-logging]

